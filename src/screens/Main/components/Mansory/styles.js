import styled from "styled-components/native";
import { Animated } from "react-native";

export const Container = styled.ScrollView`
  flex: 1;
`;

export const ImageContainer = styled.View`
  display: flex;
  flex: 1;
  background-color: #d2ed;
  width: 100%;
`;
